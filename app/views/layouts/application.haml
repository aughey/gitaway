%html
  %head
    %title Gitaway
    = stylesheet_link_tag :all
    -# javascript_include_tag :defaults
    = csrf_meta_tag

  %body
    #content{:class => @contentclass || "content"}
      - if current_user
        %p== You are logged in as #{l current_user} (UserID: #{current_user.id})
      =yield
